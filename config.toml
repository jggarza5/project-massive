# ─────────────────────────────────────────────
#  project-massive
#  strategy_mode: "Mean-rev", "Trend", or "Both"
# ─────────────────────────────────────────────

[database]
host     = "localhost"
port     = "6543"
dbname   = "postgres"
user     = "postgres"
password = "password"

[data]
parquet_daily   = "C:/code/python/forex-data/daily_2009_2026.parquet"
parquet_sub     = "C:/code/python/forex-data/sub_1min_2009_2026.parquet"
start_date      = "2010-01-01"
end_date        = "2026-01-01"
sub_bar_minutes = 1

[account]
initial_equity = 10000.0

# ─────────────────────────────────────────────
#  Single run
# ─────────────────────────────────────────────

[single]
label                  = "donchian_run_reset"
strategy_mode          = "Both"
entry_donchian_period  = 10
atr_period             = 14
trigger_atr_mult       = 0.25
tp_atr_mult            = 1.0
sl_atr_mult            = 1.0
max_trades_per_day     = 1
lots                   = 0.1

# ─────────────────────────────────────────────
#  Walk-forward optimization
# ─────────────────────────────────────────────

[wfo]
strategy_mode      = "Both"
is_days            = 252
oos_days           = 63
lots               = 0.1
max_trades_per_day = 3
atr_period         = 14
entry_periods      = [10]
trigger_atr_mults  = [0.25, 0.5, 1.0]
tp_atr_mults       = [0.5, 1.0, 1.5]
sl_atr_mults       = [1.0, 1.5, 2.0]

# ─────────────────────────────────────────────
#  Sweep
# ─────────────────────────────────────────────

[sweep]
strategy_mode      = "Both"
lots               = 0.1
max_trades_per_day = 3
atr_period         = 14
entry_periods      = [10]
trigger_atr_mults  = [0.25, 0.5, 1.0]
tp_atr_mults       = [0.5, 1.0, 1.5, 2.0, 3.0]
sl_atr_mults       = [1.0, 1.5, 2.0, 3.0]